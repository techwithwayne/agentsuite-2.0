from django.apps import AppConfig


class HumancapitalConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "humancapital"
    verbose_name = "Human Capital"  # This sets the blue section name in Admin
